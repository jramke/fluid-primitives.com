# Plans

Implementation plans for future agent sessions to pick up. Each is self-contained — file paths, code sketches, and verification steps are already researched, so no prior conversation context is needed to execute them.

When a plan is picked up, update its status here and at the top of its own file. When it's done, either delete the file or mark it Done — don't let this list drift from reality.

| Plan                                                               | Summary                                                                                                                         | Status      |
| ------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------- | ----------- |
| [select-combobox-portals.md](select-combobox-portals.md)           | Make Select and Combobox render their floating content through a portal by default, like Popover/Tooltip/Dialog already do      | Not started |
| [component-parts-global-props.md](component-parts-global-props.md) | Show `class`/`asChild`/`attributes` per part in the docs Arguments tables; stop injecting `asChild` on parts where it's a no-op | Not started |
